# 値そのものを mise.toml の中で暗号化する（Direct age・experimental）。
# [env] の中身は run.sh が `mise set --age-encrypt` で書き込む。
[settings]
experimental = true

[tools]
# 版を固定する。latest のままだと上流が動いた日に測定値が変わる。
age = "1.3.2"
