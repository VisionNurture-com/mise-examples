# 別ファイルを丸ごと暗号化して読む（sops・experimental）。
# .env.json は run.sh が作って暗号化する。
[settings]
experimental = true

[tools]
# 版を固定する。latest のままだと上流が動いた日に測定値が変わる。
sops = "3.13.3"
age = "1.3.2"

[env]
_.file = { path = ".env.json", redact = true }
