#!/usr/bin/env bash
# 3 つの検査を通してからコミットする。
# 有効にするには: git config core.hooksPath hooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
node tools/check-structure.mjs
node tools/check-neutrality.mjs
